with "config";
with "utilada_sys";
with "utilada_core";
with "utilada_base";
with "utilada_curl";
with "utilada_http";

abstract project Util is
   Version := "@UTIL_VERSION@";
   Library_Dir := "lib";

end Util;
